# examples/ holds the deliberately unhardened Dockerfile that this tool's
# README shows being fixed — a full-fat base image and an apt-get install
# without --no-install-recommends are the point of the example, not a defect
# in this repo. The tool's own source is still scanned.
ignore = [
  "*/examples/*",
]
