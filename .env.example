# Copy to .env and fill in. Never commit the real .env.
# No environment variables are required — hadofix is a pure CLI with zero
# runtime dependencies, and hadolint (its engine) is found on PATH.
#
# Tests only:
# HADOFIX_UPDATE_GOLDEN=1  # rewrite the expectations under tests/golden/
