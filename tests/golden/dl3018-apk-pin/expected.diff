--- Dockerfile
+++ Dockerfile (hadofix)
@@ -2,6 +2,9 @@
 RUN apk add --no-cache curl=8.12.1-r1
 
 FROM alpine:3.22
-RUN apk add --no-cache curl
+# hadofix(DL3018): `apk add curl` installs whatever the index serves the day it
+# runs, so the same commit can build two different images. Pinned to the
+# version this repository already commits to elsewhere.
+RUN apk add --no-cache curl=8.12.1-r1
 USER 10001
 CMD ["/usr/local/bin/app"]
